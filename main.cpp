#include <iostream>
#include <string>
using namespace std;

// Milestone 1 

// Node Struct
struct Node {
    string name;
    string order;
    Node * next;
};

// Front and Back
Node * head = NULL;
Node * tail = NULL;

// Add to the Back
Node * n = new Node;{
n->name= name;
n->order = order;
n->next = NULL;
}

// Queue
void printQueue() {
    Node * temp = head;
    cout << "Coffee Booth Queue: ";
    while (temp != NULL) {

    }


int main() {
    
    return 0;
}
